/**
 * Returns empty array - default products (1-9) have been removed.
 * All products should now be added via Admin panel and stored in initial-products.json
 * t: translation object (en or ar), language: 'en' | 'ar'
 */
export function getDefaultProducts(t, language) {
  // Return empty array - no default products
  return [];
}
