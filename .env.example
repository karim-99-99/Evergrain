# EmailJS Configuration
# Get these values from https://dashboard.emailjs.com/
# 1. Sign up/login at EmailJS
# 2. Create a service (Gmail, Outlook, etc.)
# 3. Create email templates for orders and contact
# 4. Copy your Public Key, Service ID, and Template IDs here

VITE_EMAILJS_PUBLIC_KEY=your_public_key_here
VITE_EMAILJS_SERVICE_ID=your_service_id_here
VITE_EMAILJS_ORDER_TEMPLATE_ID=your_order_template_id_here
VITE_EMAILJS_CONTACT_TEMPLATE_ID=your_contact_template_id_here

# Optional: Django backend API (products). If set, frontend loads products from API instead of initial-products.json
# VITE_API_URL=http://localhost:8000

# Optional: Google Drive API key for reliable video playback. Without it, Drive videos use iframe (may show "Couldn't Preview" in some browsers).
# Get key: Google Cloud Console → APIs & Services → Credentials → Create API Key → Restrict to "Drive API"
# VITE_GOOGLE_DRIVE_API_KEY=your_api_key_here
